const size = {
  mobile: '600px',
  desktop: '1800px',
}

const theme = {
  mainColor: `#CCD6A6`,
  bgGroundColor: '#FFFFFF',
  button: '#61876E',
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
}

export default theme
